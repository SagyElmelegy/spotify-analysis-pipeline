name: 'spotify_project'
version: '1.0.0'
config-version: 2

profile: 'spotify_project'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  spotify_project:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: view
